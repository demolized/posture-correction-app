{
  "name": "posture-correction-app",
  "version": "1.0.0",
  "description": "A comprehensive, interactive Posture Correction Application with Android support",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "android": "npm run build && npx cap sync && npx cap open android",
    "android:build": "npm run build && npx cap sync && npx cap build android",
    "android:run": "npm run build && npx cap sync && npx cap run android",
    "android:sync": "npx cap sync android",
    "mobile:prepare": "npm run build && npx cap sync",
    "mobile:open": "npx cap open",
    "mobile:serve": "npx cap serve"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "lucide-react": "^0.263.1",
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.0",
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.24",
    "@capacitor/cli": "^5.0.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "keywords": [
    "posture",
    "correction",
    "wellness",
    "fitness",
    "react",
    "mobile",
    "android",
    "capacitor"
  ],
  "author": "Your Name",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/posture-correction-app.git"
  },
  "homepage": "https://yourusername.github.io/posture-correction-app"
}
